// Since we are storing tokens in local storage of the browser, we need keys to access them.
// Here we create constants for those keys.
export const ACCESS_TOKEN = 'access';
export const REFRESH_TOKEN = 'refresh'